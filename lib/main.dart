import 'package:flutter/material.dart';

void main() {
  runApp(const Tal3a());
}

class Tal3a extends StatelessWidget {
  const Tal3a({super.key});

  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
